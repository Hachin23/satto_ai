<template>
  <header 
    class="w-full bg-white border-b border-gray-200 flex items-end justify-between px-4 pb-3 z-50 shrink-0
           pt-[env(safe-area-inset-top)] h-[calc(56px+env(safe-area-inset-top))]"
  >
    <slot />
  </header>
</template>